# OPC-HARNESS 架构设计

> **文档版本**: v2.1  
> **最后更新**: 2026 年 3 月 22 日  
> **架构类型**: 混合架构 (云端AI + 本地工具)  
> **运行环境**: 用户本地计算机 + 云端AI 服务

---

## 1. 架构概述

### 1.1 架构定位

OPC-HARNESS 采用**混合架构 (Hybrid Architecture)** 设计，作为一款本地桌面应用运行在用户计算机上，核心数据存储在本地文件系统，但 AI 能力通过对接多种云端AI厂商 API 实现，同时深度集成本地 AI编码工具 CLI，提供完整的 Vibe Coding 体验。

### 1.2 核心原则

| 原则 | 说明 | 实现方式 |
|------|------|---------|
| **多云 AI 支持** | 支持多家 AI厂商，用户自主选择 | 统一 AI 适配层 + 多厂商 API 对接 |
| **本地优先存储** | 项目数据存储在本地文件系统 | SQLite + 本地文件存储 |
| **工具链集成** | 深度集成本地 AI编码工具 CLI | 进程调用 + CLI协议适配 |
| **密钥安全** | API密钥本地加密存储 | OS Keychain/Keystore (keyring-rs) |
| **离线降级** | 网络异常时有降级方案 | 本地缓存 + 离线提示 |

### 1.3 技术栈概览

```
前端：React 18 + TypeScript 5 + Tailwind CSS 3 + Zustand 4
后端：Rust + Tauri v2
数据库：SQLite (rusqlite)
AI 服务：OpenAI / Anthropic / Kimi / GLM / MiniMax
CLI工具：Kimi CLI / Claude Code / Codex CLI (规划中)
```

### 1.4 架构演进路线

```
┌─────────────────────────────────────────────────────────────┐
│  MVP 阶段 (v1.0) - 当前版本                                 │
│  ✓ 支持 5+ 主流 AI厂商 API (OpenAI/Anthropic/智谱/月之暗面等)  │
│  ✓ AI 配置管理界面完成                                       │
│  ✓ Vibe Design 模块完成 (PRD/用户画像/竞品分析)              │
│  ⚠ CLI工具集成框架完成，具体工具待接入                     │
│  ✓ 本地 SQLite 数据存储                                     │
│  ⚠ 基础工具链检测 (Node.js/Git)                             │
├─────────────────────────────────────────────────────────────┤
│  进阶阶段 (v2.0) - 规划中                                   │
│  ├─ 支持本地模型 (Ollama) 作为备选                          │
│  ├─ 多设备同步 (可选)                                        │
│  ├─ 插件系统扩展                                             │
│  ├─ AI Agent 编排 (多模型协作)                                │
│  └─ 团队协作 (局域网 P2P)                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 系统架构图

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     🖥️ 桌面应用层 (Frontend)                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  React 18 + TypeScript 5 + Tailwind CSS 3              │   │
│  │  ├─ 渲染进程：UI 界面、用户交互                          │   │
│  │  ├─ 状态管理：Zustand 4 + Immer                        │   │
│  │  ├─ 路由：React Router 6                               │   │
│  │  └─ 代码编辑：Monaco Editor (@monaco-editor/react)     │   │
│  └─────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                     🔧 主进程层 (Tauri v2 Main Process)         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│  │ 文件系统插件  │ │ 对话框插件   │ │ Shell 插件    │          │
│  │ tauri-plugin-fs│ │tauri-plugin-dialog│ │tauri-plugin-shell│ │
│  └──────────────┘ └──────────────┘ └──────────────┘          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│  │ AI 服务模块    │ │ CLI工具管理器 │ │ 数据存储服务  │          │
│  │ src/ai/      │ │ src/cli/     │ │ src/db/      │          │
│  └──────────────┘ └──────────────┘ └──────────────┘          │
├─────────────────────────────────────────────────────────────────┤
│                     🤖 云端AI 服务层 (Cloud AI)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │  月之暗面 │ │  MiniMax │ │  OpenAI  │ │ Anthropic│         │
│  │  Kimi    │ │   API    │ │   API    │ │  Claude  │         │
│  │  • k1.5  │ │  • abab6 │ │  • GPT-4o│ │  • 3.5   │         │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │
│  ┌──────────┐ ┌──────────┐                                  │
│  │  智谱 AI  │ │  其他    │                                  │
│  │  GLM     │ │          │                                  │
│  │  • GLM-4 │ │          │                                  │
│  └──────────┘ └──────────┘                                  │
├─────────────────────────────────────────────────────────────────┤
│                     🔌 本地 AI 工具 CLI 层 (规划中)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                      │
│  │ 月之暗面 │ │ Anthropic│ │  OpenAI  │                      │
│  │ Kimi CLI │ │ClaudeCode│ │  Codex   │                      │
│  │ (待接入) │ │ (待接入)  │ │ (待接入)  │                      │
│  └──────────┘ └──────────┘ └──────────┘                      │
├─────────────────────────────────────────────────────────────────┤
│                     🛠️ 本地工具链层                              │
│  ┌──────────┐ ┌──────────┐                                    │
│  │  Node.js │ │   Git    │                                    │
│  │ (运行时) │ │(版本控制)│                                    │
│  └──────────┘ └──────────┘                                    │
├─────────────────────────────────────────────────────────────────┤
│                     💾 本地数据存储层                            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│  │  SQLite DB   │ │  项目文件    │ │  配置文件    │          │
│  │  (元数据)    │ │  (代码/资源) │ │  (设置/密钥) │          │
│  │ opc-harness.db│ │ ~/OPC-Harness-Projects/ │ │ OS Keychain │ │
│  └──────────────┘ └──────────────┘ └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 AI 服务架构详解

```
┌─────────────────────────────────────────────────────────────┐
│  AI 服务统一适配层 (AI Adapter Layer)                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  前端接口 (src/types/index.ts):                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  export interface AIConfig {                            ││
│  │    provider: string                                     ││
│  │    model: string                                        ││
│  │    apiKey: string                                       ││
│  │    temperature?: number                                 ││
│  │    maxTokens?: number                                   ││
│  │    streaming?: boolean                                  ││
│  │  }                                                      ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Rust 实现 (src/ai/mod.rs):                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  pub enum AIProviderType {                              ││
│  │    OpenAI, Anthropic, Kimi, GLM, MiniMax                ││
│  │  }                                                      ││
│  │                                                         ││
│  │  pub struct AIProvider {                                ││
│  │    provider_type: AIProviderType,                       ││
│  │    api_key: String,                                     ││
│  │    client: reqwest::Client,                             ││
│  │  }                                                      ││
│  │                                                         ││
│  │  impl AIProvider {                                      ││
│  │    pub async fn chat(&self, request: ChatRequest)       ││
│  │    pub async fn validate_key(&self) -> Result<bool>     ││
│  │  }                                                      ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Tauri Commands (src/commands/ai.rs):                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  #[tauri::command]                                      ││
│  │  pub async fn validate_ai_key(...) -> Result<bool>      ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn chat(...) -> Result<ChatResponse>         ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn generate_prd(...) -> Result<PRDResponse>  ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  已实现特性:                                                 │
│  ✓ 统一接口，支持 5 家 AI厂商                                 │
│  ✓ API Key 验证功能                                          │
│  ✓ 流式输出支持 (EventSource)                               │
│  ✓ 结构化数据生成 (PRD/用户画像/竞品分析)                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 CLI工具集成架构 (框架已完成，待具体实现)

```
┌─────────────────────────────────────────────────────────────┐
│  CLI工具集成层 (CLI Tool Integration)                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  当前状态：框架已完成，具体工具待接入                        │
│                                                              │
│  架构设计 (src/cli/mod.rs):                                  │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  pub struct CLISession {                                ││
│  │    id: String,                                          ││
│  │    tool_type: String,                                   ││
│  │    project_path: String,                                ││
│  │  }                                                      ││
│  │                                                         ││
│  │  // Session Manager (使用 lazy_static + Mutex)          ││
│  │  static ref SESSIONS: Arc<Mutex<HashMap<String, CLISession>>> ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  Tauri Commands (src/commands/cli.rs):                      │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  #[tauri::command]                                      ││
│  │  pub async fn detect_tools() -> DetectToolsResponse     ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn create_cli_session(...) -> session_id     ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn send_cli_prompt(...)                      ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn read_cli_output(...)                      ││
│  │  #[tauri::command]                                      ││
│  │  pub async fn stop_cli_session(...)                     ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  规划支持的工具:                                             │
│  • Kimi CLI - 月之暗面官方工具 (中文优秀)                   │
│  • Claude Code - Anthropic官方工具 (英文强大)              │
│  • Codex CLI - OpenAI官方工具 (多模态)                     │
│  • OpenCode - 开源工具 (多模型支持)                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 技术栈选择

### 3.1 桌面应用框架

**已选择：Tauri v2** 

选择理由:
- ✅ 包体积小 (相比 Electron 小 10-20 倍)
- ✅ Rust 后端，性能优异
- ✅ 安全性好 (内存安全、系统权限控制)
- ✅ 跨平台支持 (Windows/macOS/Linux)
- ✅ 适合频繁调用本地 CLI工具的场景

### 3.2 完整技术栈

```
┌─────────────────────────────────────────────────────────────┐
│  前端层 (Frontend) - src/                                   │
│  ├─ 框架：React 18 + TypeScript 5                          │
│  ├─ 样式：Tailwind CSS 3 + shadcn/ui (Radix UI)            │
│  ├─ 状态管理：Zustand 4 + Immer                            │
│  ├─ 路由：React Router 6                                   │
│  ├─ 编辑器：Monaco Editor (@monaco-editor/react)           │
│  ├─ Markdown 渲染：react-markdown + remark-gfm             │
│  └─ HTTP 客户端：Axios + Fetch                              │
├─────────────────────────────────────────────────────────────┤
│  应用层 (Tauri v2) - src-tauri/                             │
│  ├─ 后端：Rust 2021 Edition                                │
│  ├─ IPC:Tauri Commands + Events                           │
│  ├─ HTTP 客户端：reqwest 0.12 (支持 JSON 和流式)              │
│  ├─ 进程管理：tokio::process (异步运行时)                 │
│  ├─ 数据库：rusqlite 0.32 (SQLite)                         │
│  ├─ 密钥存储：keyring-rs 3 (OS Keychain)                   │
│  │  ├─ macOS: Keychain Services                            │
│  │  ├─ Windows: Windows Credential Manager                 │
│  │  └─ Linux: Secret Service API / libsecret               │
│  ├─ 时间处理：chrono (serde 集成)                           │
│  ├─ 错误处理：thiserror + anyhow                            │
│  └─ 工具库：uuid, lazy_static, futures                     │
├─────────────────────────────────────────────────────────────┤
│  云端AI 服务对接                                            │
│  ├─ 协议：REST API + SSE (Server-Sent Events)              │
│  ├─ 认证：API Key / Bearer Token                           │
│  ├─ 格式：OpenAI API兼容格式 (行业事实标准)                │
│  └─ 流式：text/event-stream (EventSource)                  │
├─────────────────────────────────────────────────────────────┤
│  本地 AI 工具 CLI (规划中)                                   │
│  ├─ 进程通信：Stdio (stdin/stdout/stderr)                  │
│  ├─ 协议适配：各厂商CLI协议                                │
│  ├─ 会话管理：PTY (伪终端) + 进程隔离                      │
│  └─ 输出解析：正则 + 结构化日志                            │
├─────────────────────────────────────────────────────────────┤
│  本地工具链                                                 │
│  ├─ 运行时：Node.js 18+                                    │
│  ├─ 版本控制：Git 2.x                                      │
│  ├─ 容器：Docker Desktop (可选)                            │
│  └─ 编辑器：VS Code / Cursor (外部调用)                    │
├─────────────────────────────────────────────────────────────┤
│  数据存储                                                   │
│  ├─ 元数据：SQLite (opc-harness.db)                        │
│  │  ├─ projects 表                                         │
│  │  ├─ ai_configs 表                                       │
│  │  └─ cli_sessions 表                                     │
│  ├─ 缓存：本地 JSON 文件                                     │
│  ├─ 项目文件：本地文件系统                                 │
│  │  └─ ~/OPC-Harness-Projects/                             │
│  └─ 密钥：OS Keychain / Windows Credential                 │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 依赖版本清单

#### 前端依赖 (package.json)
``json
{
  "dependencies": {
    "@monaco-editor/react": "^4.6.0",
    "@radix-ui/react-slot": "^1.2.4",
    "@radix-ui/react-tabs": "^1.1.13",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "axios": "^1.6.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "immer": "^10.0.3",
    "lucide-react": "^0.344.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^9.0.1",
    "react-router-dom": "^6.22.0",
    "remark-gfm": "^4.0.0",
    "tailwind-merge": "^2.2.0",
    "tailwindcss-animate": "^1.0.7",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.55",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "postcss": "^8.4.35",
    "prettier": "^3.2.5",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
```

#### Rust 依赖 (Cargo.toml)
```toml
[dependencies]
tauri = { version = "2.0.0", features = [] }
tauri-plugin-shell = "2.0.0"
tauri-plugin-dialog = "2.0.0"
tauri-plugin-fs = "2.0.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "stream"] }
rusqlite = { version = "0.32", features = ["bundled"] }
keyring = { version = "3", features = ["apple-native", "windows-native", "linux-native"] }
async-trait = "0.1"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"
anyhow = "1"
futures = "0.3"
eventsource-stream = "0.2"
lazy_static = "1.4"
```

---

## 4. 核心模块详细设计

### 4.1 AI 适配服务 (AI Adapter Service)

#### 4.1.1 统一接口定义 (TypeScript)

``typescript
// src/types/index.ts

export interface AIProvider {
  id: string
  name: string
  baseUrl: string
  models: AIModel[]
}

export interface AIModel {
  id: string
  name: string
  maxTokens: number
  description?: string
}

export interface AIConfig {
  provider: string
  model: string
  apiKey: string
  temperature?: number
  maxTokens?: number
  streaming?: boolean
}

export interface Message {
  role: 'system' | 'user' | 'assistant'
  content: string
}

// 前端调用方式
import { invoke } from '@tauri-apps/api/core'

// 验证 API Key
const isValid = await invoke<boolean>('validate_ai_key', {
  provider: 'openai',
  api_key: 'sk-...'
})

// 发送对话
const response = await invoke<ChatResponse>('chat', {
  provider: 'openai',
  model: 'gpt-4o',
  api_key: 'sk-...',
  messages: [{ role: 'user', content: 'Hello' }],
  temperature: 0.7,
  max_tokens: 1000
})

// 生成 PRD
const prd = await invoke<PRDResponse>('generate_prd', {
  idea: '创建一个在线协作文档工具',
  provider: 'openai',
  model: 'gpt-4o',
  api_key: 'sk-...'
})
```

#### 4.1.2 Rust 后端实现

``rust
// src/ai/mod.rs

use async_trait::async_trait;
use reqwest::Client;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy)]
pub enum AIProviderType {
    OpenAI,
    Anthropic,
    Kimi,
    GLM,
    MiniMax,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ChatRequest {
    pub model: String,
    pub messages: Vec<Message>,
    pub temperature: Option<f32>,
    pub max_tokens: Option<i32>,
    pub stream: bool,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ChatResponse {
    pub content: String,
    pub model: String,
    pub usage: Option<Usage>,
}

pub struct AIProvider {
    provider_type: AIProviderType,
    api_key: String,
    client: Client,
}

impl AIProvider {
    pub fn new(provider_type: AIProviderType, api_key: String) -> Self {
        Self {
            provider_type,
            api_key,
            client: Client::new(),
        }
    }

    fn get_base_url(&self) -> String {
        match self.provider_type {
            AIProviderType::OpenAI => "https://api.openai.com/v1".to_string(),
            AIProviderType::Anthropic => "https://api.anthropic.com/v1".to_string(),
            AIProviderType::Kimi => "https://api.moonshot.cn/v1".to_string(),
            AIProviderType::GLM => "https://open.bigmodel.cn/api/paas/v4".to_string(),
            AIProviderType::MiniMax => "https://api.minimax.chat/v1".to_string(),
        }
    }

    fn get_auth_header(&self) -> (String, String) {
        match self.provider_type {
            AIProviderType::Anthropic => (
                "x-api-key".to_string(),
                self.api_key.clone(),
            ),
            _ => (
                "Authorization".to_string(),
                format!("Bearer {}", self.api_key),
            ),
        }
    }

    pub async fn validate_key(&self) -> Result<bool, AIError> {
        // Simple validation - make a test request
        match self.provider_type {
            AIProviderType::OpenAI => {
                let response = self.client
                    .get(format!("{}/models", self.get_base_url()))
                    .header(self.get_auth_header().0, self.get_auth_header().1)
                    .send()
                    .await
                    .map_err(|e| AIError { message: e.to_string() })?;
                
                Ok(response.status().is_success())
            }
            _ => {
                // For other providers, assume valid for now
                Ok(true)
            }
        }
    }

    pub async fn chat(&self, request: ChatRequest) -> Result<ChatResponse, AIError> {
        match self.provider_type {
            AIProviderType::OpenAI => self.chat_openai(request).await,
            AIProviderType::Anthropic => self.chat_anthropic(request).await,
            AIProviderType::Kimi => self.chat_kimi(request).await,
            AIProviderType::GLM => self.chat_glm(request).await,
            AIProviderType::MiniMax => self.chat_minimax(request).await,
        }
    }
}
```

#### 4.1.3 Tauri Commands 封装

``rust
// src/commands/ai.rs

use crate::ai::{AIProvider, AIProviderType, ChatRequest};

#[tauri::command]
pub async fn validate_ai_key(
    provider: String,
    api_key: String,
) -> Result<bool, String> {
    let provider_type = parse_provider_type(&provider)?;
    let provider = AIProvider::new(provider_type, api_key);
    
    provider.validate_key()
        .await
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn chat(
    provider: String,
    model: String,
    api_key: String,
    messages: Vec<Message>,
    temperature: Option<f32>,
    max_tokens: Option<i32>,
) -> Result<ChatResponse, String> {
    let provider_type = parse_provider_type(&provider)?;
    let provider = AIProvider::new(provider_type, api_key);
    
    let request = ChatRequest {
        model,
        messages,
        temperature: temperature.unwrap_or(0.7),
        max_tokens,
        stream: false,
    };
    
    provider.chat(request)
        .await
        .map_err(|e| e.to_string())
}

#[tauri::command]
pub async fn generate_prd(
    idea: String,
    provider: String,
    model: String,
    api_key: String,
) -> Result<PRDResponse, String> {
    // 构造系统提示词和用户输入
    let system_prompt = "你是一个专业的产品专家，擅长编写产品需求文档...";
    
    let messages = vec![
        Message { role: "system".to_string(), content: system_prompt.to_string() },
        Message { role: "user".to_string(), content: idea },
    ];
    
    // 调用 AI 服务
    let response = chat(
        provider, model, api_key, messages,
        Some(0.7), Some(2000)
    ).await?;
    
    // 解析响应为结构化 PRD
    let prd: PRDResponse = serde_json::from_str(&response.content)
        .map_err(|e| format!("解析 PRD 失败：{}", e))?;
    
    Ok(prd)
}
```

#### 4.1.3 各厂商API配置

``typescript
// AI厂商配置

export const AI_PROVIDERS = {
  openai: {
    name: 'OpenAI',
    baseUrl: 'https://api.openai.com/v1',
    models: [
      { id: 'gpt-4o', name: 'GPT-4o', maxTokens: 128000 },
      { id: 'gpt-4o-mini', name: 'GPT-4o Mini', maxTokens: 128000 },
      { id: 'o1-preview', name: 'o1 Preview', maxTokens: 128000 },
      { id: 'o1-mini', name: 'o1 Mini', maxTokens: 128000 },
    ],
    headers: (key: string) => ({
      'Authorization': `Bearer ${key}`,
      'Content-Type': 'application/json',
    }),
  },
  
  anthropic: {
    name: 'Anthropic',
    baseUrl: 'https://api.anthropic.com/v1',
    models: [
      { id: 'claude-3-5-sonnet-20241022', name: 'Claude 3.5 Sonnet', maxTokens: 200000 },
      { id: 'claude-3-opus-20240229', name: 'Claude 3 Opus', maxTokens: 200000 },
      { id: 'claude-3-sonnet-20240229', name: 'Claude 3 Sonnet', maxTokens: 200000 },
      { id: 'claude-3-haiku-20240307', name: 'Claude 3 Haiku', maxTokens: 200000 },
    ],
    headers: (key: string) => ({
      'x-api-key': key,
      'anthropic-version': '2023-06-01',
      'Content-Type': 'application/json',
    }),
  },
  
  kimi: {
    name: '月之暗面 (Kimi)',
    baseUrl: 'https://api.moonshot.cn/v1',
    models: [
      { id: 'kimi-k1.5', name: 'Kimi K1.5', maxTokens: 256000 },
      { id: 'kimi-k1', name: 'Kimi K1', maxTokens: 128000 },
    ],
    headers: (key: string) => ({
      'Authorization': `Bearer ${key}`,
      'Content-Type': 'application/json',
    }),
  },
  
  glm: {
    name: '智谱AI (GLM)',
    baseUrl: 'https://open.bigmodel.cn/api/paas/v4',
    models: [
      { id: 'glm-4-plus', name: 'GLM-4 Plus', maxTokens: 128000 },
      { id: 'glm-4', name: 'GLM-4', maxTokens: 128000 },
      { id: 'glm-4-air', name: 'GLM-4 Air', maxTokens: 128000 },
      { id: 'codegeex-4', name: 'CodeGeeX-4', maxTokens: 128000 },
    ],
    headers: (key: string) => ({
      'Authorization': key, // GLM使用API Key直接作为Authorization
      'Content-Type': 'application/json',
    }),
  },
  
  minimax: {
    name: 'MiniMax',
    baseUrl: 'https://api.minimax.chat/v1',
    models: [
      { id: 'abab6.5s-chat', name: 'abab6.5s', maxTokens: 8192 },
      { id: 'abab6-chat', name: 'abab6', maxTokens: 8192 },
      { id: 'abab5.5-chat', name: 'abab5.5', maxTokens: 16384 },
    ],
    headers: (key: string) => ({
      'Authorization': `Bearer ${key}`,
      'Content-Type': 'application/json',
    }),
  },
  
  codex: {
    name: 'OpenAI Codex',
    baseUrl: 'https://api.openai.com/v1', // 或本地CLI
    models: [
      { id: 'codex', name: 'Codex CLI', maxTokens: 128000, isCLI: true },
    ],
  },
};
```

### 4.2 CLI工具集成服务

#### 4.2.1 当前实现状态

**框架已完成，具体工具待接入**。已实现的基础功能:

``rust
// src/commands/cli.rs

use std::collections::HashMap;
use std::sync::Arc;
use tokio::sync::Mutex;
use uuid::Uuid;

// 全局会话管理器
lazy_static::lazy_static! {
    static ref SESSIONS: Arc<Mutex<HashMap<String, CLISession>>> = 
        Arc::new(Mutex::new(HashMap::new()));
}

pub struct CLISession {
    pub id: String,
    pub tool_type: String,
    pub project_path: String,
}

// 工具检测命令
#[tauri::command]
pub async fn detect_tools() -> Result<DetectToolsResponse, String> {
    let tools = vec![
        ToolStatus {
            name: "Node.js".to_string(),
            is_installed: true,
            version: Some("18.17.0".to_string()),
            install_url: Some("https://nodejs.org".to_string()),
        },
        ToolStatus {
            name: "Git".to_string(),
            is_installed: true,
            version: Some("2.42.0".to_string()),
            install_url: Some("https://git-scm.com".to_string()),
        },
        ToolStatus {
            name: "Kimi CLI".to_string(),
            is_installed: false,  // TODO: 实际检测
            version: None,
            install_url: Some("https://www.moonshot.cn/docs/cli".to_string()),
        },
        ToolStatus {
            name: "Claude Code".to_string(),
            is_installed: false,  // TODO: 实际检测
            version: None,
            install_url: Some("https://docs.anthropic.com/en/docs/agents-and-tools/claude-code".to_string()),
        },
    ];
    
    Ok(DetectToolsResponse { tools })
}

// 创建 CLI 会话
#[tauri::command]
pub async fn create_cli_session(
    request: CreateSessionRequest,
) -> Result<CreateSessionResponse, String> {
    let session_id = Uuid::new_v4().to_string();
    
    let session = CLISession {
        id: session_id.clone(),
        tool_type: request.tool_type,
        project_path: request.project_path,
    };
    
    SESSIONS.lock().await.insert(session_id.clone(), session);
    
    Ok(CreateSessionResponse { session_id })
}

// 发送 Prompt 到 CLI
#[tauri::command]
pub async fn send_cli_prompt(
    session_id: String,
    prompt: String,
) -> Result<(), String> {
    // TODO: 实际实现需要启动子进程并通信
    let sessions = SESSIONS.lock().await;
    let session = sessions.get(&session_id)
        .ok_or("Session not found")?;
    
    // 这里需要实现实际的进程调用逻辑
    println!("Sending prompt to {} CLI: {}", session.tool_type, prompt);
    
    Ok(())
}

// 读取 CLI 输出
#[tauri::command]
pub async fn read_cli_output(
    session_id: String,
) -> Result<ReadOutputResponse, String> {
    // TODO: 从子进程 stdout/stderr 读取输出
    Ok(ReadOutputResponse {
        stdout: None,
        stderr: None,
    })
}

// 停止 CLI 会话
#[tauri::command]
pub async fn stop_cli_session(
    session_id: String,
) -> Result<(), String> {
    SESSIONS.lock().await.remove(&session_id);
    Ok(())
}
```

#### 4.2.2 规划中的 CLI工具配置

``typescript
// src/types/index.ts - 待扩展

export interface CLITool {
  id: string
  name: string
  command: string
  installUrl: string
  description: string
  features: string[]
  protocol: 'stdio' | 'mcp'
  isInstalled?: boolean
}

// 规划支持的工具列表
export const CLI_TOOLS_CONFIG = {
  kimi: {
    name: 'Kimi CLI',
    command: 'kimi',
    installUrl: 'https://www.moonshot.cn/docs/cli',
    description: '月之暗面官方CLI工具，中文支持优秀',
    features: ['code_generation', 'file_edit', 'shell_command'],
    protocol: 'stdio' as const,
  },
  
  claude: {
    name: 'Claude Code',
    command: 'claude',
    installUrl: 'https://docs.anthropic.com/en/docs/agents-and-tools/claude-code',
    description: 'Anthropic官方CLI工具，英文场景强大',
    features: ['code_generation', 'file_edit', 'shell_command', 'git_integration'],
    protocol: 'stdio' as const,
  },
  
  codex: {
    name: 'OpenAI Codex CLI',
    command: 'codex',
    installUrl: 'https://github.com/openai/codex',
    description: 'OpenAI官方CLI工具，基于GPT-4o',
    features: ['code_generation', 'file_edit', 'shell_command', 'mcp_support'],
    protocol: 'mcp' as const, // Model Context Protocol
  },
  
  opencode: {
    name: 'OpenCode',
    command: 'opencode',
    installUrl: 'https://github.com/opencode-ai/opencode',
    description: '开源AI编码助手，支持多模型',
    features: ['code_generation', 'file_edit'],
    protocol: 'stdio' as const,
  },
}
```

#### 4.2.3 前端 CLI 控制台组件 (规划)

``typescript
// src/components/vibe-coding/CLITerminal.tsx - 待实现

import React, { useEffect, useRef, useState } from 'react'
import { invoke } from '@tauri-apps/api/core'

interface CLITerminalProps {
  sessionId: string
  toolType: string
}

export const CLITerminal: React.FC<CLITerminalProps> = ({
  sessionId,
  toolType,
}) => {
  const [output, setOutput] = useState<string[]>([])
  const [input, setInput] = useState('')
  const [isRunning, setIsRunning] = useState(true)
  const outputEndRef = useRef<HTMLDivElement>(null)

  // 轮询读取 CLI 输出
  useEffect(() => {
    const interval = setInterval(async () => {
      try {
        const [stdout, stderr] = await invoke<[string | null, string | null]>(
          'read_cli_output',
          { sessionId }
        )
        
        if (stdout) {
          setOutput(prev => [...prev, stdout])
        }
        if (stderr) {
          setOutput(prev => [...prev, `[ERROR] ${stderr}`])
        }
      } catch (error) {
        console.error('Failed to read CLI output:', error)
      }
    }, 100)

    return () => clearInterval(interval)
  }, [sessionId])

  // 自动滚动到底部
  useEffect(() => {
    outputEndRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [output])

  const handleSend = async () => {
    if (!input.trim()) return
    
    setOutput(prev => [...prev, `> ${input}`])
    
    try {
      await invoke('send_cli_prompt', {
        sessionId,
        prompt: input,
      })
      setInput('')
    } catch (error) {
      setOutput(prev => [...prev, `[ERROR] ${error}`])
    }
  }

  return (
    <div className="flex flex-col h-full bg-slate-900 rounded-lg overflow-hidden">
      {/* 工具栏 */}
      <div className="flex items-center justify-between px-4 py-2 bg-slate-800 border-b border-slate-700">
        <span className="text-sm font-medium text-slate-200">
          {CLI_TOOLS_CONFIG[toolType]?.name || toolType}
        </span>
        <button
          onClick={async () => {
            await invoke('stop_cli_session', { sessionId })
            setIsRunning(false)
          }}
          disabled={!isRunning}
          className="px-3 py-1 text-xs bg-red-600 hover:bg-red-700 text-white rounded disabled:opacity-50"
        >
          停止
        </button>
      </div>

      {/* 输出区域 */}
      <div className="flex-1 overflow-y-auto p-4 font-mono text-sm">
        {output.map((line, index) => (
          <div key={index} className="mb-1 text-slate-300">
            {line}
          </div>
        ))}
        <div ref={outputEndRef} />
      </div>

      {/* 输入区域 */}
      <div className="flex items-center gap-2 p-4 border-t border-slate-700">
        <span className="text-slate-500">&gt;</span>
        <input
          type="text"
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={(e) => e.key === 'Enter' && handleSend()}
          disabled={!isRunning}
          placeholder="输入指令或询问 AI..."
          className="flex-1 bg-transparent text-slate-200 outline-none"
        />
        <button
          onClick={handleSend}
          disabled={!isRunning || !input.trim()}
          className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded disabled:opacity-50"
        >
          发送
        </button>
      </div>
    </div>
  )
}
```

---

## 5. 数据存储架构

### 5.1 数据库设计

#### 5.1.1 数据库初始化

``rust
// src/db/mod.rs

use rusqlite::{Connection, Result};
use std::path::PathBuf;
use tauri::Manager;

pub async fn init_database(app_handle: &tauri::AppHandle) -> Result<()> {
    // 获取应用数据目录
    let app_dir = app_handle.path().app_data_dir().unwrap_or_else(|_| PathBuf::from("."));
    
    // 确保目录存在
    if let Err(e) = std::fs::create_dir_all(&app_dir) {
        return Err(rusqlite::Error::SqliteFailure(
            rusqlite::ffi::Error::new(1),
            Some(format!("Failed to create app data directory: {}", e))
        ));
    }
    
    let db_path = app_dir.join("opc-harness.db");
    
    // 打开数据库连接
    let conn = Connection::open(db_path)?;
    
    // 创建项目表
    conn.execute(
        "CREATE TABLE IF NOT EXISTS projects (
            id TEXT PRIMARY KEY,
            name TEXT NOT NULL,
            description TEXT,
            status TEXT DEFAULT 'idea',
            progress INTEGER DEFAULT 0,
            created_at TEXT NOT NULL,
            updated_at TEXT NOT NULL,
            idea TEXT,
            prd TEXT,
            user_personas TEXT,
            competitor_analysis TEXT
        )",
        [],
    )?;
    
    // 创建 AI 配置表
    conn.execute(
        "CREATE TABLE IF NOT EXISTS ai_configs (
            provider TEXT PRIMARY KEY,
            model TEXT NOT NULL,
            api_key TEXT NOT NULL
        )",
        [],
    )?;
    
    // 创建 CLI 会话表
    conn.execute(
        "CREATE TABLE IF NOT EXISTS cli_sessions (
            id TEXT PRIMARY KEY,
            tool_type TEXT NOT NULL,
            project_path TEXT NOT NULL,
            created_at TEXT NOT NULL
        )",
        [],
    )?;
    
    Ok(())
}
```

#### 5.1.2 数据库 Schema 详解

``sql
-- 项目表：存储用户项目信息
CREATE TABLE projects (
    id TEXT PRIMARY KEY,              -- UUID
    name TEXT NOT NULL,                -- 项目名称
    description TEXT,                  -- 项目描述
    status TEXT DEFAULT 'idea',        -- 阶段：idea/design/coding/marketing/completed
    progress INTEGER DEFAULT 0,        -- 进度百分比
    created_at TEXT NOT NULL,          -- 创建时间 (ISO 8601)
    updated_at TEXT NOT NULL,          -- 更新时间 (ISO 8601)
    idea TEXT,                         -- 初始想法
    prd TEXT,                          -- PRD 文档 (JSON 字符串)
    user_personas TEXT,                -- 用户画像 (JSON 数组)
    competitor_analysis TEXT           -- 竞品分析 (JSON 对象)
);

-- AI 配置表：存储各厂商 AI 配置
CREATE TABLE ai_configs (
    provider TEXT PRIMARY KEY,         -- 厂商 ID: openai/anthropic/kimi/glm/minimax
    model TEXT NOT NULL,               -- 默认模型 ID
    api_key TEXT NOT NULL              -- API Key (加密存储)
);

-- CLI 会话表：记录 CLI工具使用情况
CREATE TABLE cli_sessions (
    id TEXT PRIMARY KEY,               -- UUID
    tool_type TEXT NOT NULL,           -- 工具类型：kimi/claude/codex
    project_path TEXT NOT NULL,        -- 项目路径
    created_at TEXT NOT NULL           -- 创建时间
);
```

### 5.2 文件系统结构

```
~/.opc-harness/                          # 应用数据目录
├── opc-harness.db                       # SQLite 数据库
│
└── config.json                          # 应用配置 (可选)
    {
      "defaultAIProvider": "openai",
      "defaultCLITool": "kimi",
      "projectRoot": "~/OPC-Harness-Projects",
      "theme": "dark",
      "shortcuts": {...}
    }

~/OPC-Harness-Projects/                  # 用户项目目录 (可配置)
├── solo-flow-20260321/                  # 项目文件夹
│   ├── .opc-harness/                    # 项目元数据
│   │   ├── manifest.json                # 项目配置
│   │   ├── prd.md                       # PRD 文档 (Markdown)
│   │   ├── user-personas.json           # 用户画像
│   │   └── competitor-analysis.md       # 竞品分析
│   │
│   ├── src/                             # 项目源代码
│   ├── design/                          # 设计稿
│   └── marketing/                       # 营销素材
│
└── blog-platform-20260322/
    └── ...
```

### 5.3 密钥管理

#### 5.3.1 安全存储方案

``rust
// 使用 keyring-rs 进行跨平台密钥存储

use keyring::Entry;

// 存储 API Key
fn store_api_key(provider: &str, api_key: &str) -> Result<(), keyring::Error> {
    let entry = Entry::new("OPC-HARNESS", provider)?;
    entry.set_password(api_key)?;
    Ok(())
}

// 获取 API Key
fn get_api_key(provider: &str) -> Option<String> {
    let entry = Entry::new("OPC-HARNESS", provider).ok()?;
    entry.get_password().ok()
}

// 删除 API Key
fn delete_api_key(provider: &str) -> Result<(), keyring::Error> {
    let entry = Entry::new("OPC-HARNESS", provider)?;
    entry.delete_credential()?;
    Ok(())
}
```

#### 5.3.2 跨平台密钥存储位置

```
    date DATE DEFAULT CURRENT_DATE,
    UNIQUE(provider, model, date)
);

-- 应用设置表
CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 8. 工具检测与安装引导

### 8.1 已实现工具检测

``rust
// src/commands/cli.rs

#[tauri::command]
pub async fn detect_tools() -> Result<DetectToolsResponse, String> {
    // 当前返回硬编码状态，待实现真实检测
    let tools = vec![
        ToolStatus {
            name: "Node.js".to_string(),
            is_installed: true,
            version: Some("18.17.0".to_string()),
            install_url: Some("https://nodejs.org".to_string()),
        },
        ToolStatus {
            name: "Git".to_string(),
            is_installed: true,
            version: Some("2.42.0".to_string()),
            install_url: Some("https://git-scm.com".to_string()),
        },
        ToolStatus {
            name: "Kimi CLI".to_string(),
            is_installed: false,  // TODO: 实际检测
            version: None,
            install_url: Some("https://www.moonshot.cn/docs/cli".to_string()),
        },
        ToolStatus {
            name: "Claude Code".to_string(),
            is_installed: false,  // TODO: 实际检测
            version: None,
            install_url: Some("https://docs.anthropic.com/en/docs/agents-and-tools/claude-code".to_string()),
        },
    ];
    
    Ok(DetectToolsResponse { tools })
}
```

### 8.2 前端展示组件

``typescript
// src/components/vibe-coding/ToolDetection.tsx - 待实现

export function ToolDetection() {
  const [tools, setTools] = useState<CLITool[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    invoke<DetectToolsResponse>('detect_tools')
      .then((response) => {
        setTools(response.tools as unknown as CLITool[])
        setLoading(false)
      })
      .catch((error) => {
        console.error('Failed to detect tools:', error)
        setLoading(false)
      })
  }, [])

  return (
    <div className="space-y-4">
      <h2 className="text-xl font-bold">AI编码工具</h2>
      <p className="text-muted-foreground">
        选择一个 AI编码工具来帮助你开发项目
      </p>

      <div className="grid gap-4">
        {tools.map((tool) => (
          <Card key={tool.id}>
            <CardHeader>
              <div className="flex items-center justify-between">
                <div>
                  <CardTitle>{tool.name}</CardTitle>
                  <CardDescription>{tool.description}</CardDescription>
                </div>
                {tool.isInstalled ? (
                  <Badge variant="default" className="bg-green-500">
                    已安装 {tool.version}
                  </Badge>
                ) : (
                  <Button variant="outline" size="sm" asChild>
                    <a href={tool.installUrl} target="_blank" rel="noopener noreferrer">
                      安装
                    </a>
                  </Button>
                )}
              </div>
            </CardHeader>
            <CardContent>
              <div className="flex gap-2">
                {tool.features.map((feature) => (
                  <Badge key={feature} variant="secondary">
                    {feature}
                  </Badge>
                ))}
              </div>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  )
}
```

---

## 9. 安全架构

### 9.1 API密钥安全管理

``rust
// 使用 keyring-rs 进行系统级密钥存储

use keyring::Entry;

// 存储 API Key 到系统钥匙串
fn store_api_key(provider: &str, api_key: &str) -> Result<(), keyring::Error> {
    let entry = Entry::new("OPC-HARNESS", provider)?;
    entry.set_password(api_key)?;
    Ok(())
}

// 从系统钥匙串获取 API Key
fn get_api_key(provider: &str) -> Option<String> {
    let entry = Entry::new("OPC-HARNESS", provider).ok()?;
    entry.get_password().ok()
}

// 删除 API Key
fn delete_api_key(provider: &str) -> Result<(), keyring::Error> {
    let entry = Entry::new("OPC-HARNESS", provider)?;
    entry.delete_credential()?;
    Ok(())
}
```

**跨平台存储位置:**

| 平台 | 存储位置 | 访问方式 |
|------|---------|---------|
| macOS | Keychain Services | Keychain Access.app |
| Windows | Windows Credential Manager | 控制面板 > 凭据管理器 |
| Linux | Secret Service API | gnome-keyring / kwallet |

### 9.2 网络安全策略

``rust
// 仅允许连接到受信任的 AI厂商 API

impl AIProvider {
    fn get_base_url(&self) -> String {
        match self.provider_type {
            AIProviderType::OpenAI => "https://api.openai.com/v1".to_string(),
            AIProviderType::Anthropic => "https://api.anthropic.com/v1".to_string(),
            AIProviderType::Kimi => "https://api.moonshot.cn/v1".to_string(),
            AIProviderType::GLM => "https://open.bigmodel.cn/api/paas/v4".to_string(),
            AIProviderType::MiniMax => "https://api.minimax.chat/v1".to_string(),
        }
    }
}

// 禁止行为:
// ✗ 不会上传用户代码到第三方服务器
// ✗ 不会收集用户行为数据
// ✗ 不会主动连接非 AI厂商的服务
```

### 9.3 Tauri 安全配置

``json
// src-tauri/tauri.conf.json

{
  "app": {
    "security": {
      "csp": null,
      "capabilities": [
        {
          "identifier": "default",
          "local": true,
          "windows": ["main"],
          "permissions": [
            "core:default",
            "shell:allow-open",
            "dialog:allow-open",
            "dialog:allow-save",
            "fs:default",
            "fs:allow-read-text-file",
            "fs:allow-write-text-file"
          ]
        }
      ]
    }
  }
}
```

---

## 10. 性能与成本优化

### 10.1 智能模型选择策略

``typescript
// 根据任务类型推荐合适的模型

const MODEL_RECOMMENDATIONS = {
  // PRD生成 - 需要强大的理解和结构化能力
  prd_generation: {
    recommended: 'gpt-4o',
    alternatives: ['claude-3-5-sonnet', 'kimi-k1.5'],
    budget: 'gpt-4o-mini',
  },
  
  // 代码生成 - 需要代码专业能力
  code_generation: {
    recommended: 'codex',
    alternatives: ['claude-3-5-sonnet', 'codegeex-4'],
    budget: 'gpt-4o-mini',
  },
  
  // 营销文案 - 需要创意和多样性
  marketing_copy: {
    recommended: 'claude-3-5-sonnet',
    alternatives: ['gpt-4o', 'kimi-k1.5'],
    budget: 'glm-4-air',
  },
  
  // 简单问答 - 快速响应优先
  simple_qa: {
    recommended: 'gpt-4o-mini',
    alternatives: ['glm-4-air', 'abab6.5s'],
    budget: 'gpt-4o-mini',
  },
}
```

### 10.2 响应缓存机制

``rust
// TODO: 实现 AI响应缓存

use std::collections::HashMap;
use std::time::{SystemTime, UNIX_EPOCH};

struct ResponseCache {
    cache: HashMap<String, CachedResponse>,
    ttl_seconds: u64,
}

struct CachedResponse {
    content: String,
    timestamp: u64,
}

impl ResponseCache {
    fn new(ttl_hours: u64) -> Self {
        Self {
            cache: HashMap::new(),
            ttl_seconds: ttl_hours * 3600,
        }
    }

    fn get(&self, key: &str) -> Option<String> {
        if let Some(cached) = self.cache.get(key) {
            let now = SystemTime::now()
                .duration_since(UNIX_EPOCH)
                .unwrap()
                .as_secs();
            
            if now - cached.timestamp < self.ttl_seconds {
                return Some(cached.content.clone());
            }
        }
        None
    }

    fn set(&mut self, key: String, content: String) {
        let timestamp = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .unwrap()
            .as_secs();
        
        self.cache.insert(key, CachedResponse { content, timestamp });
    }
}
```

### 10.3 Token 优化策略

``rust
// 自动截断超长上下文

fn truncate_messages(messages: Vec<Message>, max_tokens: i32) -> Vec<Message> {
    let mut total_tokens = 0;
    let mut truncated = Vec::new();
    
    // 保留 system message
    if let Some(system) = messages.iter().find(|m| m.role == "system") {
        total_tokens += estimate_tokens(&system.content);
        truncated.push(system.clone());
    }
    
    // 从后向前添加 user/assistant messages
    for message in messages.iter().rev() {
        if message.role == "system" {
            continue;
        }
        
        let msg_tokens = estimate_tokens(&message.content);
        if total_tokens + msg_tokens <= max_tokens {
            total_tokens += msg_tokens;
            truncated.push(message.clone());
        } else {
            break;
        }
    }
    
    // 反转回正确顺序
    truncated.reverse();
    truncated
}

fn estimate_tokens(text: &str) -> i32 {
    // 简化的 token 估算：每 4 个字符约 1 个 token
    (text.len() / 4) as i32
}
```

---

## 附录

### A. 技术选型总结

| 层级 | 技术 | 版本 | 选择理由 |
|------|------|------|---------|
| **桌面框架** | Tauri | v2 | 轻量、安全、Rust 性能 |
| **前端** | React | v18 | 生态成熟、类型安全 |
| **语言** | TypeScript | v5 | 类型安全、开发体验好 |
| **样式** | Tailwind CSS | v3 | 开发效率高 |
| **UI 组件** | shadcn/ui | latest | 美观、可定制 |
| **状态管理** | Zustand | v4 | 轻量、易用 |
| **路由** | React Router | v6 | 标准解决方案 |
| **后端** | Rust | 2021 | 性能、安全、CLI集成友好 |
| **HTTP 客户端** | reqwest | v0.12 | 异步、功能完善 |
| **数据库** | SQLite | rusqlite v0.32 | 零配置、单文件 |
| **密钥存储** | OS Keychain | keyring v3 | 最安全的方式 |
| **进程管理** | tokio | v1 | 异步运行时 |

### B. 支持的 AI 服务列表

#### B.1 云端 AI厂商

| 厂商 | 代表模型 | 特点 | 适用场景 |
|------|---------|------|---------|
| OpenAI | GPT-4o, o1 | 综合能力最强 | 通用任务 |
| Anthropic | Claude 3.5 Sonnet | 长上下文、代码强 | 代码生成、PRD |
| 月之暗面 | Kimi K1.5 | 中文优秀 | 中文内容 |
| 智谱 AI | GLM-4, CodeGeeX | 国产、CodeGeeX专业 | 代码生成 |
| MiniMax | abab6.5 | 性价比高 | 简单任务 |

#### B.2 规划中的 CLI工具

| 工具 | 厂商 | 特点 | 协议 | 状态 |
|------|------|------|------|------|
| Kimi CLI | 月之暗面 | 中文优秀、国内访问快 | Stdio | 待接入 |
| Claude Code | Anthropic | 功能最完善 | Stdio | 待接入 |
| Codex CLI | OpenAI | 官方工具、MCP支持 | MCP/Stdio | 待接入 |
| OpenCode | 开源 | 多模型支持 | Stdio | 待接入 |

### C. 开发命令参考

``bash
# 前端开发
npm run dev              # 启动 Vite 开发服务器 (端口 1420)
npm run build            # 构建生产版本
npm run preview          # 预览构建结果

# Tauri 开发
npm run tauri:dev        # 启动 Tauri 开发环境 (前后端一起)
npm run tauri:build      # 构建生产安装包

# 代码质量
npm run lint             # ESLint 检查
npm run lint:fix         # 自动修复 ESLint 问题
npm run format           # Prettier 格式化
npm run format:check     # 检查格式规范

# Harness Engineering
npm run harness:check    # 架构健康检查
npm run harness:gc       # 垃圾回收 (清理过时文档、死代码)
npm run harness:gc:dry   # 预演垃圾回收 (不实际删除)
```

### D. 项目目录结构

```
opc-harness/
├── src/                          # 前端代码
│   ├── components/
│   │   ├── common/               # 通用组件
│   │   │   ├── AIConfig.tsx
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Header.tsx
│   │   │   └── Sidebar.tsx
│   │   ├── ui/                   # shadcn/ui 组件
│   │   │   ├── badge.tsx
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   └── ...
│   │   ├── vibe-design/          # Vibe Design 模块
│   │   │   ├── IdeaInput.tsx
│   │   │   ├── PRDDisplay.tsx
│   │   │   ├── UserPersonas.tsx
│   │   │   └── CompetitorAnalysis.tsx
│   │   ├── vibe-coding/          # Vibe Coding 模块
│   │   │   └── CodingWorkspace.tsx
│   │   └── vibe-marketing/       # Vibe Marketing 模块
│   │       └── MarketingStrategy.tsx
│   ├── stores/                   # Zustand stores
│   │   ├── aiConfigStore.ts
│   │   ├── appStore.ts
│   │   └── projectStore.ts
│   ├── types/                    # TypeScript 类型
│   │   └── index.ts
│   ├── lib/                      # 工具函数
│   │   └── utils.ts
│   ├── App.tsx                   # 应用入口
│   ├── main.tsx                  # React 入口
│   └── index.css                 # 全局样式
├── src-tauri/                    # Rust 后端
│   ├── src/
│   │   ├── main.rs               # Rust 入口
│   │   ├── commands/             # Tauri Commands
│   │   │   ├── ai.rs
│   │   │   ├── cli.rs
│   │   │   └── system.rs
│   │   ├── ai/                   # AI 服务
│   │   ├── cli/                  # CLI工具管理
│   │   ├── db/                   # 数据库
│   │   ├── models/               # 数据模型
│   │   ├── services/             # 业务逻辑
│   │   └── utils/                # 工具函数
│   ├── Cargo.toml                # Rust 依赖
│   └── tauri.conf.json           # Tauri 配置
├── docs/                         # 文档
│   └── 架构设计.md
├── .harness/                     # Harness Engineering
│   ├── context-engineering/
│   └── scripts/
├── package.json                  # 前端依赖
├── tsconfig.json                 # TypeScript 配置
├── vite.config.ts                # Vite 配置
└── tailwind.config.js            # Tailwind 配置
```

---

> **文档版本**: v2.1  
> **最后更新**: 2026 年 3 月 22 日  
> **维护者**: OPC-HARNESS Team
> 
> 本文档基于实际项目代码编写，反映了当前的架构实现状态。随着项目发展，部分内容可能正在规划或实现中。